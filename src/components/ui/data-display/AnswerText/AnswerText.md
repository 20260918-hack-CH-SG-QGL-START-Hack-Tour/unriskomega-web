# AnswerText

Renders a bounded answer string with paragraphs, short headings, lists, bold and code. Links and HTML remain escaped text. It accepts `text` only and has no data access or application state. Semantic list and heading markup preserves reading order.
