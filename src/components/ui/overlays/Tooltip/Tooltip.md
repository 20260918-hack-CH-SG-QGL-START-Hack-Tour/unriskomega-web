# Tooltip

Provides explanatory `label` text for one button child. The child retains its ref, accessible name and events. Opens on hover or keyboard focus, remains hoverable, closes on Escape and never moves focus. `align="end"` keeps right-edge controls inside the viewport. Use the button's `aria-label` for its action name; tooltip text supplies the additional explanation.
